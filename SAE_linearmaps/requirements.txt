# Install these AFTER a RunPod PyTorch base image (which already ships torch + CUDA).
# NOTE: `pip install sae_lens` bumps torch, and the DEFAULT PyPI wheel tracks the newest CUDA --
# which can be newer than the host driver. Seen 2026-08-11: it installed torch 2.13.0+cu130 onto a
# 12.8-driver host, so torch.cuda.is_available() went False and every script fell back to CPU.
# setup.sh now hard-fails on that; the fix is to reinstall torch from a wheel index matching the
# driver (`--index-url https://download.pytorch.org/whl/cu128`, or cu124).
# Once a run works end-to-end, freeze exact versions:  pip freeze > requirements.lock.txt
sae_lens
transformer_lens
datasets
huggingface_hub
matplotlib
numpy
scikit-learn
spacy
