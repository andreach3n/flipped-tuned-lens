# Generated SAE outputs / artifacts — regenerated on every run on each machine,
# so they collide between Mac and the GPU box. Keep them local; don't version them.
communities*.txt
sae.pt
*.pt

# Cloned dependency, not our source code.
dictionary_learning/
.env
__pycache__/
*.pyc

# Extracted delphi result cells (~800 MB). Re-downloadable from
# andreayhchen/pythia-1b-heap-replication with `hf_hub_download` + tarfile — see
# SAE_linearmaps/randomized/pythia/README.md. The analysis scripts read them via RESULTS_DIR.
SAE_linearmaps/randomized/pythia/delphi_results/
